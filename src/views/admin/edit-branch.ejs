<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Branch</title>
    <style>
        body {
            background-color: #f5f5f5; /* Màu nền xám nhạt */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white; /* Màu nền trắng cho container */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333; /* Màu chữ tối cho tiêu đề */
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #555; /* Màu chữ xám cho nhãn */
        }
        input, select {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc; /* Đường viền xám nhạt */
            border-radius: 5px;
            background-color: #f9f9f9; /* Màu nền cho input */
            transition: border-color 0.3s;
        }
        input:focus, select:focus {
            border-color: #007bff; /* Đổi màu đường viền khi focus */
            outline: none; /* Bỏ outline mặc định */
        }
        button {
            width: 100%;
            padding: 10px;
            background-color: #007bff; /* Màu xanh dương cho nút */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #0056b3; /* Đổi màu khi hover */
        }
        .cancel-button {
            background-color: #dc3545; /* Màu đỏ cho nút hủy */
        }
        .cancel-button:hover {
            background-color: #c82333; /* Đổi màu khi hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Edit Branch</h1>
        <form action="/admin/branches/edit/<%= branch.branch_id %>" method="POST">
            <label for="branch_name">Branch Name</label>
            <input type="text" id="branch_name" name="branch_name" value="<%= branch.branch_name %>" required>

            <label for="address">Address</label>
            <input type="text" id="address" name="address" value="<%= branch.address %>" required>

            <label for="opening_time">Opening Time</label>
            <input type="time" id="opening_time" name="opening_time" value="<%= new Date(branch.opening_time).toLocaleTimeString('en-GB', { hour12: false }) %>" required>

            <label for="closing_time">Closing Time</label>
            <input type="time" id="closing_time" name="closing_time" value="<%= new Date(branch.closing_time).toLocaleTimeString('en-GB', { hour12: false }) %>" required>

            <label for="status">Status</label>
            <select id="status" name="status" required>
                <option value="active" <%= branch.status === 'active' ? 'selected' : '' %>>Active</option>
                <option value="inactive" <%= branch.status === 'inactive' ? 'selected' : '' %>>Inactive</option>
            </select>

            <label for="phone_number">Phone Number</label>
            <input type="text" id="phone_number" name="phone_number" value="<%= branch.phone_number %>" required>

            <label for="has_motorbike_parking">Has Motorbike Parking</label>
            <select id="has_motorbike_parking" name="has_motorbike_parking" required>
                <option value="true" <%= branch.has_motorbike_parking ? 'selected' : '' %>>Yes</option>
                <option value="false" <%= !branch.has_motorbike_parking ? 'selected' : '' %>>No</option>
            </select>
            <label for="has_car_parking">Has Car Parking</label>
            <select id="has_car_parking" name="has_car_parking" required>
                <option value="true" <%= branch.has_car_parking ? 'selected' : '' %>>Yes</option>
                <option value="false" <%= !branch.has_car_parking ? 'selected' : '' %>>No</option>
            </select>

            <label for="area_id">Area ID</label>
            <input type="number" id="area_id" name="area_id" value="<%= branch.area_id %>" required>

            <label for="manager">Manager ID</label>
            <input type="number" id="manager" name="manager" value="<%= branch.manager %>" required>

            <button type="submit">Save Changes</button>
            <button type="button" class="cancel-button" onclick="window.location.href='/admin/branches'">Cancel</button>
        </form>
    </div>
    <script></script>
</body>
</html>